#!/usr/bin/env bash
set -euo pipefail

git fetch origin
git checkout -B dana-test --no-track origin/main
git commit --allow-empty -m "Empty commit on dana-test"
git push --force --set-upstream origin dana-test
